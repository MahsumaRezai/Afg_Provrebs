import { Fragment } from 'react';
import './main.css'; // CSS file

const Card = (props) => {
    return (
        <Fragment>
            <div className="Card">
            </div>
        </Fragment>
    )
}

export default Card